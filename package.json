{
  "name": "staking-rewards",
  "version": "0.0.4",
  "description": "This project can be use as a starting po...",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/sP34ND/subql-module4-aggregation",
  "repository": "github:sP34ND/subql-module4-aggregation",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "sP34ND",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}
